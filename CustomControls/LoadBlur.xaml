<UserControl x:Class="Yt_Downloader.CustomControls.LoadBlur"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Yt_Downloader.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Panel.ZIndex="1" CornerRadius="20" Width="200" Height="100" TextBlock.TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center" Background="#b1a7a6">
            <Grid>
                <TextBlock Margin="0,0,0,30" FontWeight="Bold" VerticalAlignment="Center" Text="Downloading..." Foreground="Black"/>
                <ProgressBar x:Name="progress" Width="180" Height="30" 
                             Minimum="0" Maximum="100" Margin="0,30,0,0" />
            </Grid>
        </Border>
        <Border Panel.ZIndex="0" Background="LightGray" Opacity="0.7" CornerRadius="24">
            <Border.Effect>
                <BlurEffect Radius="2" KernelType="Gaussian" RenderingBias="Quality" />
            </Border.Effect>
        </Border>
    </Grid>
</UserControl>
